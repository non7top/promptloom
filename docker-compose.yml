services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /app
    init: true
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - DISPLAY=${DISPLAY:-}
    tty: true

volumes:
  node_modules:
