repos:
  - repo: local
    hooks:
      - id: lint
        name: biome lint (docker)
        description: Run biome lint inside the dev container, same as CI.
        entry: docker compose run --rm dev npm run lint
        language: system
        files: \.(ts|tsx)$
        pass_filenames: false
